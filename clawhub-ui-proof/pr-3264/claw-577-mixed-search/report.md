# Canonical mixed skill search proof

The real local ClawHub app at `http://127.0.0.1:3017/skills?query=skill` returned a native ClawHub lexical match before an external skills.sh row carrying 9M lifetime installs. Both desktop and mobile captures preserve the API order and show the external source label and canonical route.

Fixture: isolated local Convex deployment `local-amantus-clawdhub-9`. No generated HTML or synthetic mockup was used.
